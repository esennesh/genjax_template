# The NumPyro `tracer` module (importance-trace ELBO/IWAE/OVIS/VarGrad zoo) has
# been removed; GenJAX's own inference machinery (genjax.inference, genjax.adev)
# is used instead. GenJAX-native inference helpers will live here.
